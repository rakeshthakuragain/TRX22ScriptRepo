pipeline {
    agent none

    stages {
        stage('Build') {
            
            steps {
                echo 'Building..'
                sh '''
				hostname
                '''
            }
        }
    }

    post {
        success {
            echo 'This will run only if successful'
        }
    }
}
